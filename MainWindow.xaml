<Window x:Class="element_profiles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:element_profiles"
        mc:Ignorable="d"
        ThemeMode="System"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Element Profiles" Height="450" Width="325">
    <Grid>
        <Button x:Name="ButtonAddProfile" HorizontalAlignment="Left" Margin="5,10,0,0" VerticalAlignment="Top" Height="42" Width="135" Click="ButtonAddProfile_Click">
            <Grid VerticalAlignment="Center" Width="125">
                <Image HorizontalAlignment="Left" Height="34" VerticalAlignment="Center" Width="29" Source="/Assets/icons8-add-96.png"/>
                <Label  Content="Create Profile" Margin="34,0,10,0" VerticalContentAlignment="Center" Height="34" VerticalAlignment="Center" />
            </Grid>
        </Button>
        <Button x:Name="ButtonRemoveProfile" HorizontalAlignment="Right" Margin="0,10,7,0" VerticalAlignment="Top" Height="42" Width="130" Click="ButtonRemoveProfile_Click">
            <Grid VerticalAlignment="Center" Width="120">
                <Image HorizontalAlignment="Left" Height="34" VerticalAlignment="Center" Width="29" Source="/Assets/icons8-remove-96.png"/>
                <Label  Content="Delete Profile" Margin="34,0,0,0" VerticalContentAlignment="Center" Height="34" VerticalAlignment="Top" />
            </Grid>
        </Button>
        <ListBox x:Name="ListBoxProfiles" ItemsSource="{Binding ElementProfiles}" HorizontalContentAlignment="Stretch" Margin="0,57,0,25" Background="{x:Null}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid VerticalAlignment="Center">

                        <TextBlock Text="{Binding Name}"
                            Width="150"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"/>

                        <Button x:Name="ButtonLaunch" VerticalAlignment="Center" HorizontalAlignment="Right" Content="Launch" Click="ButtonLaunch_Click" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
        <Label Content="Element Version:" HorizontalAlignment="Left" Height="25" VerticalAlignment="Bottom" Margin="5,0,0,-5"/>
        <Label x:Name="LabelElementVersion" Content="{Binding ElementVersion}" HorizontalAlignment="Left" Margin="103,0,0,-5" Width="63" Height="25" VerticalAlignment="Bottom"/>


    </Grid>
</Window>
